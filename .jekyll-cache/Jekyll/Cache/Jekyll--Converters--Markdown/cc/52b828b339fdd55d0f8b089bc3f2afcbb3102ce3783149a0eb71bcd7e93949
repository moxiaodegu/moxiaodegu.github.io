I"�<h2 id="数组去重">数组去重</h2>
<h3 id="1-嵌套循环">1. 嵌套循环</h3>
<p>```js
function unique(array) {
    var result = [];
    var arrLength = array.length;
    for (var i = 0; i &lt; arrLength; i++) {
        for (var j = 0; j &lt; result.length; j++) {
            if (array[i] === result[j]) {
                break;
            }
        }
        if (j === result.length) {
            result.push(array[i]);
        }
    }
    return result;</p>
:ET